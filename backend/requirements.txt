# ── Core Framework ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0
pydantic-settings==2.5.0

# ── ML/NLP ──
transformers==4.44.0
torch==2.4.0
tokenizers==0.19.1
sentencepiece==0.2.0

# ── Async ──
httpx==0.27.0
aiohttp==3.10.0
aiofiles==24.1.0
websockets==12.0

# ── Kafka ──
aiokafka==0.11.0

# ── Database ──
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.32
redis==5.0.8
timescaledb  # TimescaleDB extension for PostgreSQL

# ── IPFS ──
# py-ipfs-http-client==0.8.0a2  # Uncomment for IPFS integration

# ── Utilities ──
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1

# ── Testing ──
pytest==8.3.0
pytest-asyncio==0.24.0
httpx  # For test client

# ── Monitoring ──
prometheus-client==0.20.0
